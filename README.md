<pre> ``` **SQL EXPERIMENTS** EXPERIMENT 1 1. Create a database. 2. Select the current database. 3. Create an EMPLOYEE table with following structure: EMPNO NUMBER(6), ENAME VARCHAR(20), JOB VARCHAR(20), DEPTNO NUMBER(3) Allow NULL for all columns except ENAME and JOB. 4. Add a column EXPERIENCE to the EMPLOYEE table (numeric, NULL allowed). 5. Modify the column width of JOB field. 6. Create a DEPT table with structure: DEPTNO NUMBER(2), DNAME VARCHAR(20), LOC VARCHAR(20) DEPTNO as the primary key. 7. Create EMP1 table with ENAME and EMPNO, add constraint EMPNO > 100. 8. Display structure of EMPLOYEE and DEPT tables. 9. Truncate EMPLOYEE and drop DEPT table. 10. List all tables in the current database. 11. Drop column EXPERIENCE from EMPLOYEE. 12. Drop table EMP1. 13. Delete the database. ``` </pre>
